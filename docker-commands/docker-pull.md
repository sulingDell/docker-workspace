# pull docker image  -> docker hub (remote registry for docker images)

> sudo docker pull ubuntu
> sudo docker pull mysql
> sudo docker pull alpine
> sudo docker pull mongo


# verify image list
> sudo docker images